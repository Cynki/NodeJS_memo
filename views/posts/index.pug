doctype html
html
    head
        include ../partials/head.pug
    body
        include ../partials/nav.pug

        .container.mb-3
            h2.mb-3 Board
            table.board-table.table.table-sm.border-bottom
                thead.thead-light
                    tr
                        th(scope="col") Title
                        th.date(scope="col") Date
                tbody
                    if (posts == null || posts.length == 0)
                        tr
                            td(colspan=2) There is no data to show :(
                    each post in posts
                        tr
                            td
                                a(href="/posts/"+post._id)
                                    .ellipsis= post.title
                            td.date
                                span(data-date=post.createdAt)

            div
                a.btn.btn-primary(href="/posts/new") New